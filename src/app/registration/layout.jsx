export const metadata = {
    title: "Registration Page | SkillSphere",
    description:
        "Sign up for SkillSphere to unlock premium learning content. Register now to track your progress, earn certifications, and advance your career with the latest technologies.",
};

const layout = ({ children }) => {
    return <div>{children}</div>;
};

export default layout;
