
export const metadata = {
    title: "Update Profile | SkillSphere",
    description:
        "Update your personal information, profile picture, and account details to keep your SkillSphere profile accurate and up to date.",
}


const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;